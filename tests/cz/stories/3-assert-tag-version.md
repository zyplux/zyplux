# 3. [Asserting a release tag matches its target's version](3-assert-tag-version.test.ts)

## 3.1 asserting a tag against the release manifest

1. 3.1.1 logs a confirmation when the tag matches its target's declared version
2. 3.1.2 rejects a tag no release target owns
3. 3.1.3 rejects a tag whose version does not match the manifest
