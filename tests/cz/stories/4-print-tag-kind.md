# 4. [Printing a release tag's registry kind](4-print-tag-kind.test.ts)

## 4.1 classifying a tag by its release target

1. prints the registry kind of the npm target that owns the tag
2. prints the registry kind of the pypi target that owns the tag
3. prints the registry kind of the ghcr target that owns the tag

### 4.1.2 rejects a tag no release target owns
