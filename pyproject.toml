[project]
name = "sys-conf-py"
version = "0.1.0"
description = "Idempotent apt repo + key + pinning setup for a fresh Ubuntu/Kubuntu install."
requires-python = ">=3.14"
dependencies = [
    "toon-format>=0.9.0b1",
]
